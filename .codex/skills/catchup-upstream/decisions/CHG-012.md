---
id: CHG-012
upstream_range: "rust-v0.94.0 -> rust-v0.98.0"
area:
  - codex-rs
  - vscode-extension
type:
  - api
  - behavior
status: decided # draft | decided | implemented | verified
---

# Upstream変更: dynamic tool 出力の text+image 対応

## 1. 観察事実（upstreamで何が変わったか）

- 変更内容:
- dynamic tool 出力が output: string から contentItems 配列へ変更。
- text と image の複合出力に対応。
- upstreamの根拠:
  - 該当PR/コミット/リリースノート:
    - 5ea107a08
  - 仕様/挙動の要点（箇条書き）:
    - dynamic tool 出力が output: string から contentItems 配列へ変更。
    - text と image の複合出力に対応。

## 2. 影響（何がどう変わるか）

- 影響範囲（ユーザー導線）:
- app-server の API が破壊的変更。
- UI 側で contentItems を描画する必要。
- 互換性（既存設定/既存thread/既存運用）:
  - 既存設定は基本維持。破壊的変更がある場合は明示。
- 破壊的変更の有無:
  - あり
- セキュリティ/コンプライアンス観点（あれば）:
  - 追加があれば README_codez.md に明記。

## 3. 対応方針（ここを“決定”する）

### 3.1 codez（Rust/CLI/TUI/周辺ツール）

- 方針: 採用
- 理由（制約・コスト・安全性・整合性）: upstream API に追随。
- 移行方針（必要なら）: 旧形式は非互換のため明示的に破壊として扱う。

### 3.2 VSCode拡張

- 方針: 追随
- 理由（upstream整合・UX・実装制約）: 動的ツールの画像出力を表示するため。
- 代替導線（未対応なら、どう見せる/どう説明する）: なし

## 4. ユーザーが決める質問（未確定点）

- なし

## 5. 受け入れ条件（成功判定）

- CLI/TUI:
- dynamic tool の contentItems を処理できる。
- VSCode:
- VSCode で text+image が表示される。
- 互換性（既存設定で壊れない/壊れるならエラーで露呈させる）:
  - 既存設定の読み込みを維持し、破壊的変更はエラー/ドキュメントで露呈。
- ドキュメント更新:
  - `README_codez.md`:
- README_codez.md に破壊的変更として記載。
  - `vscode-extension/CHANGELOG.md`:
- vscode-extension/CHANGELOG.md に追記。

## 6. 実装メモ（決定後に埋める）

- 変更箇所: TBD
- テスト/検証コマンド: TBD
- リスクとロールバック: TBD
