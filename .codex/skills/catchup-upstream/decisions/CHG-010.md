---
id: CHG-010
upstream_range: "rust-v0.94.0 -> rust-v0.98.0"
area:
  - codex-rs
type:
  - api
  - behavior
status: decided # draft | decided | implemented | verified
---

# Upstream変更: thread/compact v2 追加

## 1. 観察事実（upstreamで何が変わったか）

- 変更内容:
- v2 RPC に thread/compact を追加（trigger-only）。
- upstreamの根拠:
  - 該当PR/コミット/リリースノート:
    - 38a47700b
  - 仕様/挙動の要点（箇条書き）:
    - v2 RPC に thread/compact を追加（trigger-only）。

## 2. 影響（何がどう変わるか）

- 影響範囲（ユーザー導線）:
- app-server に新しい RPC が追加される。
- 互換性（既存設定/既存thread/既存運用）:
  - 既存設定は基本維持。破壊的変更がある場合は明示。
- 破壊的変更の有無:
  - なし
- セキュリティ/コンプライアンス観点（あれば）:
  - 追加があれば README_codez.md に明記。

## 3. 対応方針（ここを“決定”する）

### 3.1 codez（Rust/CLI/TUI/周辺ツール）

- 方針: 採用
- 理由（制約・コスト・安全性・整合性）: upstream API に追随。
- 移行方針（必要なら）: なし

### 3.2 VSCode拡張

- 方針: 未対応
- 理由（upstream整合・UX・実装制約）: 拡張 UI への直接影響は小さい。
- 代替導線（未対応なら、どう見せる/どう説明する）: なし

## 4. ユーザーが決める質問（未確定点）

- なし

## 5. 受け入れ条件（成功判定）

- CLI/TUI:
- app-server: thread/compact v2 が実行可能。
- VSCode:
- なし
- 互換性（既存設定で壊れない/壊れるならエラーで露呈させる）:
  - 既存設定の読み込みを維持し、破壊的変更はエラー/ドキュメントで露呈。
- ドキュメント更新:
  - `README_codez.md`:
- README_codez.md に API 追加を追記（必要なら）。
  - `vscode-extension/CHANGELOG.md`:
- 必要に応じて追記

## 6. 実装メモ（決定後に埋める）

- 変更箇所: TBD
- テスト/検証コマンド: TBD
- リスクとロールバック: TBD
