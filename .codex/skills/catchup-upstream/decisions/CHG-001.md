---
id: CHG-001
upstream_range: "rust-v0.94.0 -> rust-v0.98.0"
area:
  - codex-rs
  - vscode-extension
type:
  - ui
  - behavior
  - config
status: decided # draft | decided | implemented | verified
---

# Upstream変更: Steer mode を Stable 化しデフォルト有効

## 1. 観察事実（upstreamで何が変わったか）

- 変更内容:
- Steer mode が Experimental から Stable に昇格し、デフォルト有効化。
- 実行中タスク時の入力: Enter は即送信、Tab はキュー投入。
- upstreamの根拠:
  - 該当PR/コミット/リリースノート:
    - cd5f49a61
    - 82464689c
  - 仕様/挙動の要点（箇条書き）:
    - Steer mode が Experimental から Stable に昇格し、デフォルト有効化。
    - 実行中タスク時の入力: Enter は即送信、Tab はキュー投入。

## 2. 影響（何がどう変わるか）

- 影響範囲（ユーザー導線）:
- TUI/CLI の入力操作が変わる（Enter/Tab の役割）。
- 既存ユーザーに操作感の変更が出る。
- 互換性（既存設定/既存thread/既存運用）:
  - 既存設定は基本維持。破壊的変更がある場合は明示。
- 破壊的変更の有無:
  - なし
- セキュリティ/コンプライアンス観点（あれば）:
  - 追加があれば README_codez.md に明記。

## 3. 対応方針（ここを“決定”する）

### 3.1 codez（Rust/CLI/TUI/周辺ツール）

- 方針: 採用
- 理由（制約・コスト・安全性・整合性）: upstream デフォルトに合わせ、UI/操作の差分を減らす。
- 移行方針（必要なら）: 特になし（既定値変更）。

### 3.2 VSCode拡張

- 方針: 追随
- 理由（upstream整合・UX・実装制約）: upstream と入力挙動の一貫性を確保。
- 代替導線（未対応なら、どう見せる/どう説明する）: なし

## 4. ユーザーが決める質問（未確定点）

- なし

## 5. 受け入れ条件（成功判定）

- CLI/TUI:
- TUI: 実行中 Enter 即送信 / Tab キューが有効。
- VSCode:
- VSCode: 実行中 Enter 即送信 / Tab キュー相当が有効。
- 互換性（既存設定で壊れない/壊れるならエラーで露呈させる）:
  - 既存設定の読み込みを維持し、破壊的変更はエラー/ドキュメントで露呈。
- ドキュメント更新:
  - `README_codez.md`:
- README_codez.md に変更点を記載。
  - `vscode-extension/CHANGELOG.md`:
- vscode-extension/CHANGELOG.md に挙動変更を追記。

## 6. 実装メモ（決定後に埋める）

- 変更箇所: TBD
- テスト/検証コマンド: TBD
- リスクとロールバック: TBD
